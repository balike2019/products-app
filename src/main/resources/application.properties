
server.port=8084
spring.jpa.generate-ddl=true
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
# Specify the MySQL database URL
spring.datasource.url=jdbc:mysql://localhost:3306/productService-db

# Specify the username and password for the MySQL database
spring.datasource.username=root
spring.datasource.password=Pvi@2021

# Configure the Hibernate dialect for MySQL
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.datasource.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect

   spring.mvc.pathmatch.matching-strategy = ANT_PATH_MATCHER

